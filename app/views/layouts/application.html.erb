<!DOCTYPE html>
<html>
  <head>
    <title>Acción Catolica</title>
    <meta charset="UTF-8" />
    <%= favicon_link_tag('favicon.png') %>
    <meta name="viewport" content="width=device-width" />
    <%= render 'layouts/styles_javascripts' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="page">
      <%= render 'layouts/header' %>
      <%= render 'layouts/headline' %>
      <%= render 'layouts/intr' %>
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
